<form method="post" action="{{ path('app_ressources_delete', {'id': ressource.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource.id) }}">
    <button class="btn btn-danger">🗑 Supprimer</button>
</form>
