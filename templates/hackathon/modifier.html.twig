{% extends 'base.html.twig' %}

{% block title %}Modifier le Hackathon{% endblock %}

{% block body %}
    <h1>Modifier le Hackathon "{{ hackathon.nom_hackathon }}"</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer les modifications</button>
    {{ form_end(form) }}
{% endblock %}