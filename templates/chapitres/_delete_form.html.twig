<form method="post" action="{{ path('app_chapitres_delete', {'id': chapitre.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chapitre.id) }}">
    <button class="btn btn-danger">🗑 Supprimer</button>
</form>
