{% extends 'base.html.twig' %}

{% block title %}Détail ressource{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">📄 Détail de la ressource</h1>

    <table class="table table-bordered table-hover">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ ressource.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ ressource.titre }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ ressource.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ressource.description }}</td>
            </tr>
            <tr>
                <th>Date d'ajout</th>
                <td>{{ ressource.dateAjout|date('d/m/Y H:i') }}</td>
            </tr>
            <tr>
                <th>Validée</th>
                <td>
                    {% if ressource.valide %}
                        <span class="badge bg-success">Oui</span>
                    {% else %}
                        <span class="badge bg-danger">Non</span>
                    {% endif %}
                </td>
            </tr>
        </tbody>
    </table>

    <div class="mt-3 d-flex gap-2">
        <a href="{{ path('app_ressources_edit', {'id': ressource.id}) }}" class="btn btn-warning">
            ✏️ Modifier
        </a>

        {{ include('ressources/_delete_form.html.twig') }}

        <a href="{{ path('app_ressources_new') }}" class="btn btn-outline-secondary">
            ← Ajouter une autre
        </a>
    </div>
</div>
{% endblock %}
